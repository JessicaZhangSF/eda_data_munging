# eda_data_munging
learning how to use functions such as

• data.info() \
• data.sort_values() \
• data.describe() \
• data.split() \
• data.groupby()[].agg() \
• CDF & PDF to view distribution of data by a given dimension

